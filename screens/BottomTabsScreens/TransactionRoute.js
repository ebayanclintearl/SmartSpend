import { StyleSheet, Text, View } from 'react-native';

export const TransactionRoute = () => {
  return (
    <View style={styles.container}>
      <Text>TransactionRoute</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
